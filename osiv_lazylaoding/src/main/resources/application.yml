server:
  shutdown: graceful
  port: 5001

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    open-in-view: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root-pass
    url: jdbc:mysql://localhost:3306/metabus?serverTimeZone=UTC&characterEncoding=UTF-8