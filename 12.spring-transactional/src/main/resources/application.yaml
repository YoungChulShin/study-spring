server:
  shutdown: graceful

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: user
    password: user_password
    url: jdbc:mysql://localhost:3306/my-db
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false

