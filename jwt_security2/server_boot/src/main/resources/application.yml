spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springVue
    username: root
    password: 1323
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true

custom:
  clientId: myApp
  clientSecret: secret
  jwtKey: test