spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/school-reactive-db
    username: root
    password: secret
    pool:
      enabled: true

logging:
  level:
    org:
      springframework:
        r2dbc: DEBUG