spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost/querydsl
    username: root
    password: secret
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
        format_sql: true
        # use_sql_comments: true
logging:
  level:
    org.hibernate.SQL: debug
